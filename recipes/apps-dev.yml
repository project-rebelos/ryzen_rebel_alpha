# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
---
# Development tools - optional, remove from recipe.yml if not needed

type: rpm-ostree
install:
  # Version control
  - git
  - git-lfs

  # Build essentials - REMOVED gcc/gcc-c++ due to base image version conflict
  - make
  - cmake
  - clang
  - autoconf
  - automake
  - libtool
  - pkgconfig

  # Python - REMOVED python3-virtualenv (EL9 package, needs Python 3.9)
  - python3
  - python3-pip
  - python3-devel

  # Scripting - REMOVED yq (not available), REMOVED ShellCheck (verify availability)
  - jq

  # Misc - REMOVED meld (EL9 package, needs Python 3.9)
  - diffutils
  - patch
